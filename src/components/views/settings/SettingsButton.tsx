export default function () {
    return (
        <button type="button" className="btn btn-primary-bd settings-btn" data-bs-toggle="modal"
                data-bs-target="#settingsModal">
            <i className="bi bi-gear-fill fs-4"></i>
        </button>
    )
}